language: python

python:
    - 2.6
    - 2.7

env:
    - JSON=simplejson
    - JSON=json
install:
    -
    - python setup.py install
    - pip install mock --use-mirrors
    - bash -c "test $JSON = simplejson && pip install simplejson"

script:
    - trial test_stimpy
